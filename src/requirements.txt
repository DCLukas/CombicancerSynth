# Single venv for the full synthetic-SNDS -> OMOP pipeline.
# generator stack: pandas/networkx pinned to newest versions with arm64 wheels
#   that still satisfy the generator's internal constraints (pandas<2, networkx<3).
# combicancer stack: pyspark + support libs; adlfs/azure-identity unused but
#   required by combicancer's fonc.py imports.
numpy==1.24.4
pandas==1.5.3
networkx==2.8.8
scikit-learn==1.3.2
python-Levenshtein==0.25.1
Faker==25.2.0
pyarrow==15.0.2
pyspark==3.5.1
pyyaml
adlfs
azure-identity
tqdm
